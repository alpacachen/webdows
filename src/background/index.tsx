import React from "react"
import styles from './index.module.less'
import { DesktopId } from "../constance"

export const Background = () => {
    return <div id={DesktopId} className={styles.bg}>
    </div>
}